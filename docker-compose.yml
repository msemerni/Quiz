version: '3'
services:
  node:
    restart: always
    build: .
    ports:
      - 5000:8000
    volumes:
      - ./:/code
    # env_file: .env
    # environment:
    #   - DB_CONNECTION=$DB_CONNECTION
    #   - DB_PORT=$DB_PORT
    #   - DB_HOST=$DB_HOST
    #   - DB_DATABASE=$DB_DATABASE
  mongo:
    image: mongo
    ports:
      - 27018:27017
    volumes:
      - mongodb:/data/db
volumes:
 mongodb:
